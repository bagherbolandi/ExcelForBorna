' ============================================================================
'  ThisWorkbook — (در ویرایشگر VBA با دابل‌کلیک روی ThisWorkbook جایگذاری کنید)
'  ============================================================================
Option Explicit

Private Sub Workbook_Open()
    On Error Resume Next
    modBorna.Borna_Init      ' شروع جلسه به‌صورت مهمان: فقط «راهنما» و «ورود» نمایش داده می‌شود
End Sub
